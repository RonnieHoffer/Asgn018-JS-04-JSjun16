const ghosts = ['Inky', 'Blinky', 'Pinky', 'Clyde'];

for (var index = 0; index < ghosts.length; index++) {
    console.log(`Pac-Man eats ${ghosts[index]}.`);
}

const airplanes = ['Cessna', 'Piper', 'Raytheon', 'Gulfstream'];

for (var index = 0; index < airplanes.length; index++) {
    console.log('I fly a ' + airplanes[index]);
}
